import Home from '../components/Home'
import Add from '../components/Add'
import Map from '../components/Map'

export const routes = {
    '/home': {
        component: Home,
    },
    '/add': {
        component: Add,
    },
    '/map': {
        component: Map,
    }
}
